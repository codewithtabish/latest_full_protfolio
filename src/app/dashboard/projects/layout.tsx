import React from 'react'

const CreateProjectRootLayout = ({ children }: { children: React.ReactNode }) => {
    return (
        <div>
            {children}

        </div>
    )
}

export default CreateProjectRootLayout
